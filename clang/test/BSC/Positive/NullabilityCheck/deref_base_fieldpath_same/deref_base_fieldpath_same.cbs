// RUN: %clang_cc1 -nullability-check=all -verify %s
// expected-no-diagnostics

struct Inner { int *_Borrow _Nullable f; };

int same_base(struct Inner *_Nonnull ps) {
  if ((*ps).f != nullptr) {
    return *(*ps).f;
  }
  return 0;
}
