// RUN: %clang -fbsc-experimental %s -o %test.output
// RUN: %test.output
// RUN: %clang -fbsc-experimental -rewrite-bsc %S/../../../../../../lib/Headers/bsc_include/future.hbs -o %T/future.h
// RUN: %clang -fbsc-experimental -rewrite-bsc %s -o %t-rw.c
// RUN: %clang -fbsc-experimental %t-rw.c -o %t-rw.output
// RUN: %t-rw.output
// expected-no-diagnostics

#include "future.hbs"
#include <stdlib.h>

typedef _Trait Future<int>* (*pollFunc) (); // triger point 1, do not change

typedef struct EQueueFunc {
    pollFunc func;
} EQueueFunc;


_Async void f() { // triger point 2, do not change
    EQueueFunc f;
    _Await f.func();
}

int main() {
  return 0;
}