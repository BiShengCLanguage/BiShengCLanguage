// RUN: %clang_cc1 -fsyntax-only -verify %s
// expected-no-diagnostics

struct T1 { int *_Owned ptr; };

struct Outer {
  int *_Owned arr[3];
  struct T1 nested[2];
};

void bar(int *_Owned param_arr[5], struct T1 param_sa[2]) {
  (void)param_arr;
  (void)param_sa;
}
