// RUN: %clang_cc1 -fbsc-experimental -fsyntax-only -verify %s

// TODO: FIXME: fix the error message

void foo() {
    _Trait T** a;
    int *b = (int *)a; // expected-error{{use of undeclared identifier 'a'}}
}
