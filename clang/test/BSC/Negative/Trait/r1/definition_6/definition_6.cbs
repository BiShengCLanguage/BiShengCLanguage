// RUN: %clang_cc1 -fbsc-experimental -fsyntax-only -verify %s

_Trait F {
  int a(); // expected-error {{the first parameter of _Trait member function must be 'This* this'}}
};

int main() {
  return 0;
}
