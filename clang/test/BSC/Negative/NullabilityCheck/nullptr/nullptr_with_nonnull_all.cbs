// RUN: %clang_cc1 -nullability-check=all -fsyntax-only -verify %s

// Test that _Nonnull correctly rejects nullptr assignments and usage
// with -nullability-check=all

// _Nonnull in function parameters
void take_nonnull(int * _Nonnull p) {
  (void)p;
}

void test_nonnull_args(void) {
  take_nonnull(nullptr); // expected-error {{cannot pass a possibly-null status pointer to a '_Nonnull' parameter}}
  take_nonnull((int *)nullptr); // expected-error {{cannot pass a possibly-null status pointer to a '_Nonnull' parameter}}
}

// _Nonnull function return
int * _Nonnull return_nonnull_nullptr(void) {
  return nullptr; // expected-error {{cannot return a possibly-null status pointer as a '_Nonnull' pointer}}
}

void * _Nonnull return_void_nonnull_nullptr(void) {
  return (void *)nullptr; // expected-error {{cannot return a possibly-null status pointer as a '_Nonnull' pointer}}
}

// _Nonnull with _Borrow parameter
void take_nonnull_borrow(int * _Nonnull _Borrow p) {
  (void)p;
}

void test_nonnull_borrow_args(void) {
  take_nonnull_borrow(nullptr); // expected-error {{cannot pass a possibly-null status pointer to a '_Nonnull' parameter}}
  take_nonnull_borrow((int * _Borrow)nullptr); // expected-error {{cannot cast a possibly-null status pointer to a '_Nonnull' pointer}} expected-error {{cannot pass a possibly-null status pointer to a '_Nonnull' parameter}}
}

// _Nonnull in safe zone
_Safe void test_safe_nonnull_assignment(void) {
  int * _Nonnull p1 = (int *)nullptr; // expected-error {{cannot assign a possibly-null status pointer to a '_Nonnull' pointer}}
  void * _Nonnull p2 = (void *)nullptr; // expected-error {{cannot assign a possibly-null status pointer to a '_Nonnull' pointer}}
  char * _Nonnull p3 = (char *)nullptr; // expected-error {{cannot assign a possibly-null status pointer to a '_Nonnull' pointer}}
  (void)p1;
  (void)p2;
  (void)p3;
}

// _Nonnull with const in safe zone
_Safe void test_safe_nonnull_const(void) {
  const int * _Nonnull cp = (const int *)nullptr; // expected-error {{cannot assign a possibly-null status pointer to a '_Nonnull' pointer}}
  (void)cp;
}

_Safe void test_safe_nonnull_volatile(void) {
  volatile int * _Nonnull vp = (volatile int *)nullptr; // expected-error {{cannot assign a possibly-null status pointer to a '_Nonnull' pointer}}
  (void)vp;
}
